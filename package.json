{
  "name": "address-ml-workbench",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Explainable address intelligence experiments, ranking, and demos.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "demo": "node scripts/demo.mjs",
    "evaluate": "node scripts/evaluate.mjs",
    "check": "npm test && npm run demo && npm run evaluate"
  }
}